"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {BarChart3,FolderKanban,MessageSquareText,Search,Lightbulb,FileText,Settings} from "lucide-react";
const groups=[{label:"Overview",items:[["Dashboard","/dashboard",BarChart3],["Projects","/projects",FolderKanban]]},{label:"Research",items:[["Feedback","/projects/nova-v32/analysis?tab=feedback",MessageSquareText],["Pain Points","/projects/nova-v32/analysis?tab=pain-points",Search],["Requirements","/requirements",Lightbulb]]},{label:"AI",items:[["Opportunities","/pain-points/search-experience",Lightbulb],["PRD Generator","/prd/search-optimization",FileText]]}];
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="app"><aside className="sidebar"><div className="brand">INSIGHTFLOW</div>{groups.map(g=><div key={g.label}><div className="nav-label">{g.label}</div><nav className="nav">{g.items.map(([label,href,Icon])=><Link key={label as string} href={href as string} className={path.startsWith((href as string).split("?")[0])?"active":""}><Icon size={16}/>{label as string}</Link>)}</nav></div>)}<div className="profile"><strong>Bi Penglei</strong><span>Product Manager</span><Link href="/states" style={{display:"flex",gap:8,marginTop:14,fontSize:12}}><Settings size={14}/> Demo states</Link></div></aside><main className="main"><header className="topbar"><span className="crumb">NovaNote / V3.2 User Feedback</span><div className="top-meta"><span className="badge warning">Completed with errors</span><span className="small">Updated 2m ago</span></div></header><div className="content">{children}</div></main></div>}
export function PageHeader({title,description,action}:{title:string;description?:string;action?:React.ReactNode}){return <div className="page-head"><div><div className="eyebrow">NovaNote · Research workspace</div><h1>{title}</h1>{description&&<p>{description}</p>}</div>{action}</div>}
export function Badge({children,tone="info"}:{children:React.ReactNode;tone?:"info"|"success"|"warning"|"danger"|"human"}){return <span className={`badge ${tone}`}>{children}</span>}
export function Metric({label,value,note}:{label:string;value:string|number;note:string}){return <div className="card"><div className="metric-label">{label}</div><div className="metric-value">{value}</div><div className="small">{note}</div></div>}
